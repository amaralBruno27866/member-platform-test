/**
 * Insurance Repositories Barrel Export
 *
 * Centralized export point for all Insurance repository implementations.
 * Enables clean imports across the Insurance module:
 * ```
 * import { DataverseInsuranceRepository } from '../repositories';
 * ```
 */

export { DataverseInsuranceRepository } from './dataverse-insurance.repository';
